/**
 * Information on a person that has registered to use this application.
 */
export interface User {
  /**
   * Unique identifier of this user.
   */
  id: string
  /**
   * Email address with which this user has been registered.
   */
  email: string
}
